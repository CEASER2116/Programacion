
#include <stdio.h>
#include<math.h>

/* suma cuadrados.
 el programa, al recibir como datos un grupo de entero posotivs, obtiene el
 el cuadrado de los mismo  y la suma correspodiente a dichos cuadrados,*/

 void main(void)
 {
     int NUM;
     long CUAN, SEC = 0;
     printf("\nIngrese un número entero -0 para terminar-:\t");
scanf("%d", &NUM);
 while(NUM)

/*obseva que la condicion  es verdadera mientaras el entero es diferente de cero . */
 {
     CUAN= pow (NUM, 2);
     printf("%d al cubo es %1d",)NUM, CUAN);

 SUC = SUC + CUAn;
 printf(Ingrse un numero entero -0 para terminar -:\t");

scanf("%d", &NUM);
}

 printf("n/La suma de los cuadrados es %1d",SUC);
}
